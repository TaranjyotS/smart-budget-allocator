{
  "name": "smart-budget-allocator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:install": "cd frontend && npm install --legacy-peer-deps --no-audit --no-fund",
    "frontend:build": "cd frontend && npm run build",
    "frontend:dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
