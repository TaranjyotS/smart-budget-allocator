# Google Sheets Template

Use `budget_template.csv` as the structure for your Google Sheet.

Recommended columns:

```text
type,name,category,amount,frequency,due_date,notes
```

Supported `type` values:

- income
- expense

Supported `frequency` values:

- monthly
- weekly
- biweekly
- yearly
- one-time
